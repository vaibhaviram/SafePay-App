name: safety
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1
environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0  # Required for Firebase Authentication
  cloud_firestore: ^5.6.1  # Required for firestore
  fl_chart: ^0.66.0  # For pie charts, bar graphs, and more
  upi_pay: ^1.1.0








dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


  assets:
    - lib/assets/expense_tracking.png
    - lib/assets/split_bills.png
    - lib/assets/secure_transactions.png
    - lib/assets/signup.png










